@extends('layouts.app')

@section('content')
<div class="container">
    <div class="searchbar mt-0 mb-4">
        <div class="row">
            <div class="col-md-6">
                <form>
                </form>
            </div>
            <div class="col-md-6 text-right">
                @can('create', App\Models\Rotulo::class)
                <a href="{{ route('rotulos.create') }}" class="btn btn-primary">
                    <i class="icon ion-md-add"></i> @lang('Nuevo Registro')
                </a>
                @endcan
            </div>
        </div>
    </div>

    <div class="card">
        <div class="card-body">
            <div style="display: flex; justify-content: space-between;">
                <h4 class="card-title">@lang('Lista de rótulos')</h4>
            </div>

            <div class="table-responsive">
                <table class="table table-borderless table-hover">
                    <thead>
                        <tr>
                            <th class="text-left">
                                @lang('Código de Barra')
                            </th>
                           
                            <th class="text-center">
                                @lang('Acción')
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @forelse($rotulos as $rotulo)
                        <tr>
                            <td>{{ $rotulo->codigo ?? '-' }}</td>
                           
                            <td class="text-center" style="width: 134px;">
                                <div
                                    role="group"
                                    aria-label="Row Actions"
                                    class="btn-group"
                                >
                                    @can('update', $rotulo)
                                    <a
                                        href="{{ route('rotulos.edit', $rotulo) }}"
                                    >
                                        <button
                                            type="button"
                                            class="btn btn-light"
                                        >
                                            <i class="icon ion-md-create"></i>
                                        </button>
                                    </a>
                                    @endcan @can('view', $rotulo)
                                    <a
                                        href="{{ route('rotulos.show', $rotulo) }}"
                                    >
                                        <button
                                            type="button"
                                            class="btn btn-light"
                                        >
                                            <i class="icon ion-md-eye"></i>
                                        </button>
                                    </a>
                                    @endcan @can('delete', $rotulo)
                                    <form
                                        action="{{ route('rotulos.destroy', $rotulo) }}"
                                        method="POST"
                                        onsubmit="return confirm('{{ __('crud.common.are_you_sure') }}')"
                                    >
                                        @csrf @method('DELETE')
                                        <button
                                            type="submit"
                                            class="btn btn-light text-danger"
                                        >
                                            <i class="icon ion-md-trash"></i>
                                        </button>
                                    </form>
                                    @endcan
                                </div>
                            </td>
                        </tr>
                        @empty
                        <tr>
                            <td colspan="3">
                                @lang('crud.common.no_items_found')
                            </td>
                        </tr>
                        @endforelse
                    </tbody>
                    <tfoot>
                        <tr>
                            <td colspan="3">{!! $rotulos->render() !!}</td>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
</div>
@endsection
